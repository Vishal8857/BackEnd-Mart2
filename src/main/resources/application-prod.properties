spring.application.name=Mart2

# Database config
spring.datasource.url=jdbc:mysql://localhost:3306/mart?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Vishal@8870
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA & Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=80

#Mail data
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bhosalevishal8857@gmail.com
spring.mail.password=jomocvpxnivyuajh

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true

#Logger
# Root log level
logging.level.root=INFO

# Application package logs
logging.level.com.product=DEBUG

# Spring Security logs
logging.level.org.springframework.security=DEBUG

# Hibernate SQL logs
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Save logs to file
logging.file.name=logs/application.log

# Optional
spring.datasource.platform=mysql
spring.h2.console.enabled=false

spring.jpa.open-in-view=false

# application.properties
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Docker Radis
spring.data.redis.host=localhost
spring.data.redis.port=6379

#Endpoints- Health check
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
